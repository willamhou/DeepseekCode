# One case per paragraph. Supported keys:
# name, task, category, assertion_bundle, skill, workdir, isolate_workdir, notes, budget,
# seed_observations,
# expect_tool, expect_tool_sequence, forbid_tool, expect_message_contains,
# expect_tool_output_contains, expect_last_tool_output_contains,
# min_tool_calls, max_tool_calls, max_failed_tools
# Common bundles:
# read_only_inspect, read_only_search,
# recovery_search_fallback, recovery_readback_then_search, recovery_diff_then_readback, recovery_search_then_readback,
# write_validate_ok, write_validate_repro_then_fix_ok, write_validate_failure_readback, write_validate_retry_ok,
# planning_todo_only, pr_review_readback, pr_fix_recovery, pr_patch_readback

name = "inspect-repo"
task = "inspect repository layout and summarize the key entrypoints"
category = "read_only"
assertion_bundle = "read_only_inspect"
workdir = "fixtures/rust-cli-mini"
notes = "Safe read-only baseline case in a fixture workdir"
budget = 6

name = "seeded-recovery"
task = "find where dispatch_subagent is implemented and inspect the code"
category = "recovery"
assertion_bundle = "recovery_search_fallback"
seed_observations = "read_file:failed:No such file or directory (os error 2) || recovery_hint:ok:after=read_file; next=search_text; reason=read_file failed, locate the correct file path before retrying the read"
budget = 4
max_tool_calls = 3

name = "seeded-lint-recovery"
task = "fix the lint failure in the Rust CLI"
category = "recovery"
assertion_bundle = "recovery_search_then_readback"
seed_observations = "run_shell:ok:meta.command_kind=lint\nmeta.exit_code=1\nmeta.result=failed\nmeta.failure_kind=lint_failure\nmeta.stderr_summary=cannot find value `dispatch_subagent` in this scope\nexit_code: 1\nstderr:\ncannot find value `dispatch_subagent` in this scope || recovery_hint:ok:after=run_shell; next=search_text; query=dispatch_subagent; reason=run_shell reported a lint failure (cannot find value `dispatch_subagent` in this scope), inspect the relevant code or diff before retrying the command"
budget = 4

name = "seeded-node-test-recovery"
task = "investigate the failing test in the JavaScript CLI"
category = "recovery"
assertion_bundle = "recovery_readback_then_search"
workdir = "fixtures/js-cli-mini"
seed_observations = "run_shell:ok:meta.command_kind=test\nmeta.exit_code=1\nmeta.result=failed\nmeta.failure_kind=test_failure\nmeta.failed_tests=test/route-benchmark.test.js::routeBenchmarkCommand routes bench\nmeta.stderr_summary=test failed\nexit_code: 1\nstderr:\ntest failed || recovery_hint:ok:after=run_shell; next=read_file; path=test/route-benchmark.test.js; reason=run_shell reported failing tests (test/route-benchmark.test.js::routeBenchmarkCommand routes bench), inspect the relevant code or diff before retrying the command"
budget = 4

name = "write-tests-parser"
task = "write tests for the parser"
category = "planning"
assertion_bundle = "planning_todo_only"
skill = "write-tests"
budget = 10

name = "isolated-write-validate"
task = "replace `a - b` with `a + b` in src/lib.rs and validate with cargo test"
category = "write_validate"
assertion_bundle = "write_validate_ok"
workdir = "fixtures/rust-write-mini"
isolate_workdir = true
notes = "Runs in a temp copy so apply_patch + run_shell can execute without mutating the source fixture"
budget = 6

name = "isolated-write-validate-recovery"
task = "replace `a - b` with `a * b` in src/lib.rs and validate with cargo test"
category = "write_validate"
assertion_bundle = "write_validate_failure_readback"
workdir = "fixtures/rust-write-mini"
isolate_workdir = true
notes = "Real failed-validation path: after diff + test failure, reread the patched file before the next fix attempt"
budget = 6
expect_last_tool_output_contains = "2     a * b"

name = "isolated-write-validate-retry"
task = "replace `a - b` with `a * b` in src/lib.rs and validate with cargo test until the tests pass"
category = "write_validate"
assertion_bundle = "write_validate_retry_ok"
workdir = "fixtures/rust-write-mini"
isolate_workdir = true
notes = "Real multi-step retry path: after the failed validation readback, issue one corrective patch and rerun validation"
budget = 8
expect_tool_output_contains = "read_file:2     a * b"

name = "isolated-python-write-validate"
task = "replace `a - b` with `a + b` in src/math_ops.py and validate with pytest"
category = "write_validate"
assertion_bundle = "write_validate_ok"
workdir = "fixtures/python-write-mini"
isolate_workdir = true
notes = "Real isolated Python write+validate case"
budget = 6

name = "isolated-js-write-validate-retry"
task = "replace `a - b` with `a * b` in src/math.js and validate with npm test until the tests pass"
category = "write_validate"
assertion_bundle = "write_validate_retry_ok"
workdir = "fixtures/js-write-mini"
isolate_workdir = true
notes = "Real JavaScript retry path: read the failing test file, issue one corrective patch, and rerun validation"
budget = 8
expect_tool_output_contains = "read_file:add returns the sum"

name = "isolated-go-write-validate"
task = "replace `a - b` with `a + b` in calc.go and validate with go test ./..."
category = "write_validate"
assertion_bundle = "write_validate_ok"
workdir = "fixtures/go-write-mini"
isolate_workdir = true
notes = "Real isolated Go write+validate case"
budget = 6

name = "seeded-pr-review"
task = "Review pull request #42 'Route benchmark command' on owner/repo. Highlight correctness risks, security concerns, and style violations. Output a markdown report."
category = "pr_workflow"
skill = "pr-review"
assertion_bundle = "pr_review_readback"
workdir = "fixtures/rust-cli-mini"
notes = "Seeded PR review flow over a small Rust CLI diff"
seed_observations = "git_diff:ok:diff --git a/src/cli/app.rs b/src/cli/app.rs\n@@\n-        Some(\"benchmark\") => route_benchmark_subcommand(),\n+        Some(\"bench\") => route_benchmark_subcommand(),\n         Some(\"doctor\") => \"doctor\",\n || list_files:ok:src/cli/app.rs\nsrc/main.rs"
budget = 4

name = "seeded-pr-ci-lint"
task = "CI job `lint-rust` (run #556) on PR #42 failed at step `cargo clippy`. Reproduce locally, fix the root cause, and rerun the failing check. Failed log tail follows."
category = "pr_workflow"
assertion_bundle = "recovery_search_then_readback"
workdir = "fixtures/rust-cli-mini"
notes = "Seeded CI lint failure that should search from the log-tail symbol before reading code"
seed_observations = "run_shell:ok:meta.command_kind=lint\nmeta.exit_code=1\nmeta.result=failed\nmeta.failure_kind=lint_failure\nmeta.stderr_summary=cannot find function `route_benchmark_subcommand` in this scope\nexit_code: 1\nstderr:\ncannot find function `route_benchmark_subcommand` in this scope || recovery_hint:ok:after=run_shell; next=search_text; query=route_benchmark_subcommand; reason=run_shell reported a lint failure (cannot find function `route_benchmark_subcommand` in this scope), inspect the relevant code or diff before retrying the command"
budget = 4

name = "seeded-pr-patch"
task = "Address review feedback or apply the requested change in PR #42 'Route benchmark command'. PR diff is the current head; propose minimal additional changes."
category = "pr_workflow"
assertion_bundle = "pr_patch_readback"
workdir = "fixtures/rust-cli-mini"
notes = "Seeded PR patch flow that should read the changed file from the diff without searching the PR title"
seed_observations = "git_diff:ok:diff --git a/src/cli/app.rs b/src/cli/app.rs\n@@\n-        Some(\"benchmark\") => route_benchmark_subcommand(),\n+        Some(\"bench\") => route_benchmark_subcommand(),\n         Some(\"doctor\") => \"doctor\",\n || list_files:ok:src/cli/app.rs\nsrc/main.rs"
budget = 4

name = "seeded-pr-fix-js"
task = "CI job `test-js` (run #557) on PR #43 failed at step `npm test`. Reproduce locally, fix the root cause, and rerun the failing test. Failed log tail follows."
category = "pr_workflow"
assertion_bundle = "pr_fix_recovery"
workdir = "fixtures/js-cli-mini"
notes = "Seeded JavaScript PR fix flow that should read the failing test file directly"
seed_observations = "run_shell:ok:meta.command_kind=test\nmeta.exit_code=1\nmeta.result=failed\nmeta.failure_kind=test_failure\nmeta.failed_tests=test/route-benchmark.test.js::routeBenchmarkCommand routes bench\nmeta.stderr_summary=test failed\nexit_code: 1\nstderr:\ntest failed || recovery_hint:ok:after=run_shell; next=read_file; path=test/route-benchmark.test.js; reason=run_shell reported failing tests (test/route-benchmark.test.js::routeBenchmarkCommand routes bench), inspect the relevant code or diff before retrying the command"
budget = 4

name = "fixture-pr-fix-validate-js-cli-failing-mini"
task = "CI job `test-js` (run #558) on PR #43 failed. Replace `run bench` with `run benchmark` in src/index.js and rerun npm test."
category = "pr_workflow"
assertion_bundle = "write_validate_ok"
workdir = "fixtures/js-cli-failing-mini"
isolate_workdir = true
notes = "Real JavaScript PR fix+validate case over a naturally failing CLI fixture"
budget = 6

name = "fixture-pr-reproduce-fix-rust-cli-failing-mini"
task = "CI job `test-rust` (run #559) on PR #44 failed. Reproduce locally, replace `route_bench_subcommand()` with `route_benchmark_subcommand()` in src/cli/app.rs, and rerun cargo test."
category = "pr_workflow"
assertion_bundle = "write_validate_repro_then_fix_ok"
workdir = "fixtures/rust-cli-failing-mini"
isolate_workdir = true
notes = "Real Rust PR workflow repair case that first reproduces the failing command before applying the explicit fix"
budget = 8

name = "fixture-pr-patch-validate-rust-mini"
task = "Address PR #44 review feedback: replace `a - b` with `a + b` in src/lib.rs and validate with cargo test."
category = "pr_workflow"
assertion_bundle = "write_validate_ok"
workdir = "fixtures/rust-write-mini"
isolate_workdir = true
notes = "Real PR workflow patch+validate case over an isolated Rust fixture"
budget = 6

name = "fixture-pr-retry-validate-rust-mini"
task = "Address PR #45 review feedback: replace `a - b` with `a * b` in src/lib.rs and validate with cargo test until the tests pass."
category = "pr_workflow"
assertion_bundle = "write_validate_retry_ok"
workdir = "fixtures/rust-write-mini"
isolate_workdir = true
notes = "Real PR workflow retry case over an isolated Rust fixture"
budget = 8
expect_tool_output_contains = "read_file:2     a * b"

name = "fixture-pr-retry-validate-js-mini"
task = "Address PR #46 review feedback: replace `a - b` with `a * b` in src/math.js and validate with npm test until the tests pass."
category = "pr_workflow"
assertion_bundle = "write_validate_retry_ok"
workdir = "fixtures/js-write-mini"
isolate_workdir = true
notes = "Real PR workflow JavaScript retry case over an isolated fixture"
budget = 8
expect_tool_output_contains = "read_file:add returns the sum"

name = "fixture-pr-second-round-feedback-rust-mini"
task = "Address second-round PR #47 review feedback: replace `a * b` with `a + b` in src/lib.rs and validate with cargo test."
category = "pr_workflow"
assertion_bundle = "write_validate_ok"
workdir = "fixtures/rust-review-feedback-mini"
isolate_workdir = true
notes = "Real PR workflow second-round review feedback case over an isolated Rust fixture with an already-wrong prior patch"
budget = 6

name = "fixture-pr-patch-validate-go-mini"
task = "Address PR #48 review feedback: replace `a - b` with `a + b` in calc.go and validate with go test ./..."
category = "pr_workflow"
assertion_bundle = "write_validate_ok"
workdir = "fixtures/go-write-mini"
isolate_workdir = true
notes = "Real PR workflow Go patch+validate case over an isolated fixture"
budget = 6

name = "fixture-pr-followup-rust-cli-mini"
task = "Review pull request #42 'Route benchmark command'. Inspect the touched files before summarizing risks."
category = "pr_workflow"
workdir = "fixtures/rust-cli-mini"
notes = "Seeded PR workflow follow-up should continue from child files even without explicit subagent wording"
seed_observations = "dispatch_subagent:ok:meta.child_outcome=ok\nmeta.child_files=src/cli/app.rs,src/main.rs\nmeta.child_final_message=read the CLI routing files || read_file:ok:   1 pub fn cli_from_argv(args: &[String]) -> &'static str {"
budget = 4
expect_tool = "read_file"
expect_tool_sequence = ["read_file"]
expect_tool_output_contains = "read_file:fn main() {"
forbid_tool = "list_files"
max_tool_calls = 2
max_failed_tools = 0

name = "fixture-recover-empty-search"
task = "find where `missing_fixture_symbol_20260505` is implemented, and if there are no matches inspect the repository layout instead"
category = "recovery"
assertion_bundle = "recovery_search_fallback"
workdir = "fixtures/rust-cli-mini"
notes = "Real workdir fixture for natural search_text -> list_files recovery"
budget = 6

name = "fixture-recover-empty-search-js"
task = "find where `missing_fixture_symbol_js_20260509` is implemented, and if there are no matches inspect the repository layout instead"
category = "recovery"
assertion_bundle = "recovery_search_fallback"
workdir = "fixtures/js-cli-mini"
notes = "Real JavaScript fixture for natural search_text -> list_files recovery"
budget = 6

name = "fixture-recover-failing-js-test"
task = "investigate why npm test fails in the JavaScript CLI and inspect the failing test file before retrying"
category = "recovery"
workdir = "fixtures/js-cli-failing-mini"
notes = "Natural node --test failure should reproduce and read the failing JavaScript test file"
budget = 4
expect_tool = "run_shell"
expect_tool_sequence = ["run_shell", "read_file"]
expect_tool_output_contains = "run_shell:meta.failure_kind=test_failure"
expect_last_tool_output_contains = "routeBenchmarkCommand routes bench"
max_tool_calls = 2
max_failed_tools = 0
